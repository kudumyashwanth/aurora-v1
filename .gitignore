# Regenerable build artifacts
obj_dir/
obj_tensor/
*.fst
*.vcd
*.dasm
sim_run*.log
# OpenLane run outputs (large, regenerable) — keep configs/drivers/RTL
openlane/**/runs/
# Verilator/sim build dirs (regenerable)
build_chip/
build_iso/
build_iso_chk/
build_rsys/
# OpenLane stage logs (regenerable)
openlane/**/*.log
openlane/**/gen_*.log
# Large binary IP-package artifacts — kept on disk + regenerable from the committed recipe;
# the package READMEs, LEF, lib, and SDC ARE tracked. (git stays clonable)
ip/**/*.gds
ip/**/*.spef
ip/**/*.spice
ip/**/*.def
ip/**/*.nl.v
ip/**/*.pnl.v
ip/**/*.sdf
# Large binary layout artifacts anywhere under openlane/ (regenerable; kept on disk)
openlane/**/*.gds
openlane/**/*.spef
openlane/**/*.def
openlane/rocket/DELIVERABLES_rocket_tile/
openlane/rocket/klayout_drc/
openlane/pre_check/converted/
CLAUDE.md
